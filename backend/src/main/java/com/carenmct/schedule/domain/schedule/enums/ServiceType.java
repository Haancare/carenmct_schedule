package com.carenmct.schedule.domain.schedule.enums;

/** {@code sch_service_schedules.service_type} — 앱 enum 과 동일 */
public enum ServiceType {
    visit_care,
    family_care,
    full_day_visit,
    visit_bath,
    visit_nursing,
    day_care
}
