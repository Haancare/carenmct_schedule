package com.carenmct.schedule.domain.schedule.enums;

/** {@code sch_assessment_documents.doc_type} */
public enum AssessmentDocType {
    longTerm,
    needs,
    fall,
    pressure,
    cognitive,
    carePlan
}
